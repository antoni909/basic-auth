'use strict';

const authRoutes = require('src/auth/router.js');

describe('tests login and register endpoints', ()=>{

  it('POST to /signup to create a new user',()=>{
  });
  it('POST to /signin to login as a user (use basic auth)',()=>{});
  it('Need tests for auth middleware and the routes',()=>{});
  it('Does the middleware function (send it a basic header)',()=>{});
  it('Do the routes assert the requirements (signup/signin)',()=>{});

});



